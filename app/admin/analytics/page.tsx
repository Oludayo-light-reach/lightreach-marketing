import { AdminAnalytics } from "@/components/admin/analytics";

export default function AdminAnalyticsPage() {
  return (
    <AdminAnalytics
      title="Analytics"
      description="Deep-dive tables and charts: full breakdowns by user, platform, content type, weekday, and hour (UTC). Filters are pinned on the left on large screens."
    />
  );
}
